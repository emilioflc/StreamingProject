spring:
  datasource:
    url: jdbc:mysql://localhost:3306/streamingapp_bd
    username: root
    password: pwd
    # driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format-sql: true
    show-sql: true